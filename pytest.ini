[tool:pytest]
testpaths = backend/tests
# Note: ROS plugins (launch_testing) interfere with pytest.
# Run tests with: PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest backend/tests/
filterwarnings =
    ignore::DeprecationWarning
